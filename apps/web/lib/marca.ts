/**
 * El nombre comercial está sin decidir (D1). Vive aquí, en configuración, y **no se escribe a
 * fuego en ninguna pantalla**: el día que se decida se cambia este valor y los tokens, no el
 * JSX de treinta componentes.
 */
export const NOMBRE = process.env.NEXT_PUBLIC_NOMBRE_COMERCIAL ?? 'M2G Agenda'
