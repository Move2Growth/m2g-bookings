# Tareas: DevOps / Infraestructura — **Estado: sin iniciar**

> Derivadas de la guía de lanzamiento por fase (`../README.md` §3), bloque **1.a · Cimientos**, y de ADR-0014.
> **Este rol no despliega nada.** Su producto es el entorno local y lo que Luis necesita para desplegar sin adivinar.
> Estados de tarea: `pendiente` · `en curso` · `bloqueada` · `hecha` · `validada`.

| ID | Descripción | Fase | Zona | Estado | Depende de | Criterio de aceptación |
|---|---|---|---|---|---|---|
| DEV-T001 | Esqueleto del **monorepo** (ADR-0001): `pnpm-workspace.yaml`, `package.json` de orquestación en la raíz, `pyproject.toml` con **uv** dentro de `apps/api`, y el `Makefile` con `arriba`, `abajo`, `migrar`, `semilla`, `pruebas` y `contrato`. Más el **`.gitignore`** con `.claude/worktrees/`, `__pycache__/`, `*.pyc`, `node_modules/`, `dist/`, `build/` y `.DS_Store` | 1.a | raíz, infra/local | pendiente | — | `pnpm install` y `uv sync` funcionan cada uno en su sitio, **sin intentar meter Python en el workspace de pnpm**; el `.gitignore` está antes de que el Orquestador pueda paralelizar nada |
| DEV-T002 | **`infra/local/docker-compose.yml`** con **`name: m2g-agenda` explícito** —o un `up` de este repo recrea el PostgreSQL de otro de la casa— y cinco servicios: API, **`postgis/postgis:16-3.4`**, Redis, worker y web. `make arriba` levanta todo, **aplica migraciones y carga el seed** sin un paso manual | 1.a | infra/local | pendiente | DEV-T001 | En una máquina limpia, un solo comando deja la API respondiendo y la web sirviendo; `make abajo` libera los puertos; **ningún contenedor de otro proyecto se ve afectado** |
| DEV-T003 | **Alembic** y la **migración inicial de extensiones**: `postgis`, `btree_gist` y `pgcrypto` activadas **en la migración**, no a mano. Más los **roles de base de datos**: el de la aplicación **no es dueño de las tablas y no tiene `BYPASSRLS`**, y el de lectura pública del marketplace y el del back-office son distintos (ADR-0002) | 1.a | apps/api/migraciones, infra/local | pendiente | DEV-T002 | La migración corre **desde cero contra una base vacía**; conectándose con el usuario de la aplicación, un `SELECT` sin `WHERE` **no puede saltarse una política**; comprobado conectándose, no leyendo la configuración |
| DEV-T004 | **`.env.example`** con todas las variables y **una línea explicando cada una**, más el inventario en `docs/operacion/SECRETOS-Y-VARIABLES.md`. Incluye las de los tres proveedores externos que **hoy no tienen credencial** —WhatsApp, pasarela y mapas— con su modo de desarrollo por defecto. **Nombre y propósito, nunca el valor** | 1.a | infra/local, docs/operacion | pendiente | DEV-T002 | El stack arranca **sin ninguna credencial real**; toda variable que aparece en el código está documentada en los dos sitios en la **misma sesión** |
| DEV-T005 | **El seed** (ADR-0014): un barrio de Ciudad de Panamá con negocios verosímiles —una barbería con dos barberos, un salón con cuatro profesionales **de horarios distintos entre sí**, un spa que cierra al mediodía y una manicurista independiente—, con servicios de verdad y **la agenda medio llena**, para que los huecos difíciles existan sin fabricarlos. **Idempotente, determinista y con fechas relativas a hoy** | 1.a | infra/local | pendiente | DEV-T003, ARQ-T009 | `make semilla` se puede repetir sin errores; la agenda de ejemplo **nunca sale vacía** por haber quedado en el pasado; **no aparece «Servicio 1 · 100,00» por ningún lado** |
| DEV-T006 | **`make pruebas`**: levanta la base de datos si hace falta y ejecuta la suite contra un **PostgreSQL real**. Si no hay base de datos, **falla**, no se salta las pruebas | 1.a | infra/local, raíz | pendiente | DEV-T003 | Apagando la base de datos a propósito, el comando **falla en rojo**. Salir en verde saltándose todo es el fallo conocido que esto evita |
| DEV-T007 | **`make contrato`**: regenera el OpenAPI desde la API y de ahí los tipos de `packages/api-types`, que **no se escriben a mano nunca** (ADR-0001, ADR-0012) | 1.a | packages/api-types, raíz | pendiente | DEV-T002 | Cambiar un campo en la API y regenerar **rompe en compilación** a quien lo consumía. Eso es exactamente lo que se busca |
| DEV-T008 | **README de arranque en pasos numerados**: qué instalar, qué comando, qué se ve, qué credenciales de prueba usar y qué hacer si un puerto está ocupado | 1.a | raíz | pendiente | DEV-T005, DEV-T006 | Alguien que no ha visto el repo lo levanta siguiendo los pasos y **sin preguntar nada**. Si hay que adivinar algo, no está terminado |
| DEV-T009 | Mantener el entorno cuando el modelo o el stack cambien: seed al día, variables nuevas documentadas y migraciones que sigan corriendo desde cero. Trabajo continuo | 1.b–2 | infra/local | pendiente | DEV-T005 | En cualquier momento del proyecto, `make arriba` sigue funcionando en una máquina limpia |
