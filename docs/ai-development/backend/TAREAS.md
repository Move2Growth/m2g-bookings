# Tareas: Backend — **Estado: sin iniciar**

> Derivadas de la guía de lanzamiento por fase (`../README.md` §3). **Ninguna tarea de un bloque arranca sin su especificación de Ingeniería delante**, y **BE-T009 no arranca sin las pruebas de TST-T003 escritas**.
> Estados de tarea: `pendiente` · `en curso` · `bloqueada` · `hecha` · `validada`.
> **Zonas serializadas:** `apps/api/migraciones` y `apps/api/disponibilidad`. Nunca dos agentes a la vez, ni con worktree.

| ID | Descripción | Fase | Zona | Estado | Depende de | Criterio de aceptación |
|---|---|---|---|---|---|---|
| BE-T001 | Esqueleto de la API en **FastAPI sobre Python 3.12 con uv**: `/api/v1`, salud y versión, y el **kit del contrato** que ningún módulo reinventa (ADR-0012) — identificadores **UUID v7**, paginación **por cursor**, fechas **ISO-8601 con desplazamiento explícito**, importes en **enteros de la unidad mínima con su moneda**, **forma única del error** con `codigo` estable, y el middleware de **`Idempotency-Key`**. **Sin una sola regla de negocio** | 1.a | apps/api/contrato | pendiente | DEV-T002, ARQ-T005 | Una llamada a una ruta inexistente devuelve la forma única del error; ningún identificador secuencial aparece en una respuesta; ningún importe es decimal; repetir un `POST` con la misma clave devuelve el mismo resultado |
| BE-T002 | **Primera migración y multi-tenant con RLS** (ADR-0002): tablas base con `business_id NOT NULL`, políticas contra `app.current_business_id` fijado con **`SET LOCAL` dentro de la transacción**, catálogo global **sin RLS**, y el filtro de aplicación escrito **además** de la política. Las extensiones ya vienen de DEV-T003 | 1.a | apps/api/migraciones | pendiente | DEV-T003, ARQ-T003 | La migración corre **desde cero contra una base vacía**; con el negocio A fijado, ninguna consulta devuelve filas de B **usando el rol real de la aplicación**; una conexión reutilizada del pool **no arrastra el tenant anterior** |
| BE-T003 | **Identidad** (ONB-1, ADR-0006): OTP de 6 dígitos **guardado con hash**, 5 minutos de validez, máximo 5 intentos, límite por teléfono y por IP con retroceso exponencial e invalidación de los códigos anteriores. Sesión con **acceso corto** y **refresco opaco, persistido y rotatorio**, revocable de verdad; reutilizar un refresco ya rotado **invalida toda la familia**. Identidades de Google y Apple en `auth_identities`, enlazadas **solo con correo verificado** | 1.b | apps/api/identidad | pendiente | BE-T002, ING-T001 | Un OTP caducado o con los intentos agotados **no entra**; cerrar sesión en un dispositivo surte efecto **ya**; enlazar por correo **no verificado** es imposible |
| BE-T004 | **Membresías, negocio activo y permisos** (ONB-3, STF-3, ADR-0006): una fila por usuario–negocio–rol con `dueno` y `profesional`, **hueco para `recepcion`**; el negocio activo va **en el token** y es lo que alimenta `app.current_business_id`; los permisos **se resuelven contra la membresía en cada petición**, no viajan dentro del token | 1.b | apps/api/identidad | pendiente | BE-T003 | Revocar a un profesional surte efecto **en la siguiente llamada**, no cuando caduque su token; un profesional **no accede a finanzas ni a configuración**; cambiar de negocio es **intercambiar el token**, no un parámetro que el cliente pueda manipular |
| BE-T005 | **Alta y publicación del negocio** (ONB-2, ONB-6, ONB-7, D11): datos, categorías, ubicación con pin, horario semanal, tipo y primer servicio; estados **borrador → publicado → suspendido**; el **mínimo para publicar** —1 servicio activo, horario, ubicación y 1 foto— calculado **por el servidor**, y el checklist de completitud que después alimenta el ranking | 1.b | apps/api/negocios | pendiente | BE-T004, ING-T001 | Un negocio incompleto **no se publica**, y la API dice exactamente **qué le falta**; solo lo publicado aparecerá en el marketplace; el recorrido entero se puede completar **en menos de diez minutos desde un móvil** |
| BE-T006 | **Catálogo de servicios** (SRV-1 a SRV-4): nombre, categoría global, descripción, **duración**, precio fijo o «desde» o a consultar, **buffer antes y después**, activo, foto y orden; variantes con duración y precio propios; y las categorías globales que administra M2G | 1.c | apps/api/catalogo | pendiente | BE-T005, ING-T002 | Un servicio sin duración **no existe**; el buffer se guarda **como parte del servicio** y no como hueco del calendario; los precios son enteros de la unidad mínima con su moneda |
| BE-T007 | **Equipo** (STF-1 a STF-5): profesional con foto, bio, servicios asignados, **horario propio distinto del negocio**, descansos, días libres y vacaciones; activo o inactivo, visible u oculto; profesionales **«sin cuenta»** que el dueño crea y convierte después (ONB-4); y **«cualquier profesional disponible»** con reparto de carga | 1.c | apps/api/equipo | pendiente | BE-T006, ING-T002 | Un profesional con horario distinto del negocio **es el caso normal y funciona**; un servicio que nadie presta **no se puede reservar**; «cualquiera» reparte con una regla escrita, no al azar |
| BE-T008 | **[CRÍTICA] La ocupación y la restricción de exclusión** (AGD-3, AGD-4, ADR-0004): tabla `staff_occupancy` donde **las reservas y los bloqueos son dos tipos de fila**, con `blocked_from` y `blocked_to` **generadas y persistidas por la base** incluyendo los buffers, y `EXCLUDE USING gist` con `btree_gist`, rango **semiabierto `[)`**, limitada a los estados vivos. Bloqueos **puntuales y recurrentes**. El `23P01` se traduce a **`SLOT_NO_DISPONIBLE` con 409** | 1.d | apps/api/migraciones, apps/api/reservas | pendiente | BE-T007, ARQ-T004 | **Con la comprobación de código desactivada a propósito, la base rechaza igual la segunda cita solapada**; un bloqueo de almuerzo impide reservar encima **por la misma restricción**; una cita que acaba a las 10:00 y otra que empieza a las 10:00 **no chocan**, pero con buffer de 15 minutos **sí** |
| BE-T009 | **[CRÍTICA] El motor de disponibilidad** (AGD-1, AGD-5, ADR-0003): huecos = horario del negocio ∩ horario del profesional − bloqueos − reservas − buffers, con **granularidad** —15 min por defecto—, **antelación mínima** de 1 h y **máxima** de 60 días, todas configurables por negocio. Es **el único sitio** donde se convierte una regla horaria local en instante, y donde se resuelven los cierres que cruzan medianoche. **No arranca hasta que TST-T003 esté escrita** | 1.d | apps/api/disponibilidad | pendiente | BE-T008, TST-T003 | Los ocho casos límite del encargo pasan; un servicio de 3 h **no se ofrece** en el hueco de las cinco si no cabe antes del cierre; cambiar el horario del negocio con reservas dentro **no las borra en silencio**; la respuesta lleva **la zona del negocio** además del instante · **PUERTA: aquí se para y se le enseña a Luis antes de montar pantallas encima** |
| BE-T010 | **El ciclo de la reserva** (RSV-1 a RSV-4, RSV-7): estados `pendiente` → `confirmada` → `completada` \| `no_show` \| `cancelada_cliente` \| `cancelada_negocio`, **auto-confirmación por defecto y camino manual configurable** (D10), **reprogramación como evento** en `booking_events`, ventana de cancelación del cliente configurable —2 h por defecto—, **multi-servicio encadenado como una sola fila de ocupación continua** (D13), `Idempotency-Key` obligatoria al crear, e `.ics` para añadir al calendario | 1.e | apps/api/reservas | pendiente | BE-T009, ING-T004 | Cancelar **libera el hueco de inmediato sin borrar la fila**; reprogramar deja rastro y **no es un estado final**; tres servicios seguidos ocupan **un bloque continuo** en el que no se cuela nadie; repetir el `POST` con la misma clave **no crea una segunda cita** |
| BE-T011 | **Reserva manual y ficha de cliente** (AGD-2, RSV-5, RSV-6): alta desde el mostrador o el teléfono con cliente registrado o **cliente rápido** sin cuenta; mover y reprogramar; ficha del cliente **por negocio** con historial, notas y **contador de ausencias**, y el bloqueo del reincidente | 1.e | apps/api/clientes, apps/api/reservas | pendiente | BE-T010, ING-T006 | La ficha de un cliente en el negocio A **no es visible desde B** aunque sea la misma persona; marcar un no-show incrementa **solo** el contador de ese negocio |
| BE-T012 | **La cola de notificaciones** (NTF-1 a NTF-4, ADR-0007): la tabla `notifications` **es** la cola, con **clave de idempotencia única derivada del hecho** —`recordatorio_24h:booking:{id}`—, estados y reintentos con **tope**, **decidir** separado de **entregar**, plantillas como **datos** con su nombre en Meta y su idioma, registro de entrega con coste estimado, y la interfaz `NotificationProvider` con **implementación de desarrollo** que escribe a disco y a consola | 1.f | apps/api/notificaciones | pendiente | BE-T010, ING-T005 | Encolar dos veces el mismo recordatorio **no inserta la segunda fila**; el recordatorio de 2 h de una cita ya pasada se marca **descartada**, no se reintenta para siempre; se puede contestar a «¿le llegó?» **sin adivinar**; **el stack funciona entero sin credenciales de Meta** |
| BE-T013 | **Trabajos en segundo plano con arq** (ADR-0008): `apps/worker` compartiendo código con la API, **colas separadas por criticidad** —reactiva, programada y pesada—, el planificador que **encola y no envía**, trabajos con **identificadores y no objetos**, y **fijación explícita del tenant** al abrir transacción, porque un trabajador no tiene sesión de usuario y es donde más fácil se cuela una consulta sin filtrar | 1.f | apps/worker | pendiente | BE-T012 | Ejecutar el planificador dos veces **no duplica ni un mensaje**; una exportación pesada **no retrasa** la confirmación de una reserva; ningún trabajo consulta sin tenant fijado |
| BE-T014 | **Feriados de Panamá** (AGD-6) precargados como **sugeridos, no impuestos**: el negocio puede abrir igual | 1.f | apps/api/negocios | pendiente | ARQ-T010 | La agenda sugiere el cierre y **permite ignorarlo**; la lista es un dato administrable, no código |
| BE-T015 | **Borrado de cuenta y retención** (Ley 81, con el ADR que escribe ARQ-T008 delante): borrado **desde dentro de la aplicación**, con política decidida tabla por tabla — qué se borra, qué se anonimiza y qué se conserva | 1.f | apps/api/identidad, apps/api/migraciones | pendiente | ARQ-T008 | Borrar una cuenta **no rompe la agenda de un negocio** ni deja una review huérfana con nombre; sin esto, Apple rechaza la app en la Fase 5 |
| BE-T016 | **Geografía y zonas** (MKT-6, ADR-0005): `locations.geo` como `geography(Point,4326)` con índice **GiST**, tabla `zones` jerárquica con `parent_id`, `slug` y `path` materializado, zona **persistida** en `locations.zone_id` y **editable por el dueño**, y geocoding **cacheado por texto normalizado** detrás de `GeocodingProvider` con implementación local | 2 | apps/api/marketplace, apps/api/migraciones | pendiente | BE-T005, ARQ-T009 | La búsqueda por radio usa `ST_DWithin` y ordena por `ST_Distance` **en metros**, no en grados; la zona no se recalcula en cada búsqueda; **la Fase 2 no espera al proveedor de mapas para esto** |
| BE-T017 | **Búsqueda y filtros** (MKT-1, MKT-2): texto, categoría y ubicación por GPS, dirección o zona; filtros de distancia, servicio, precio, rating, atributos, **disponibilidad real** —ahora, hoy, fecha—, abierto ahora y métodos de pago. Con el rol de **lectura pública** sobre columnas publicables, no con el rol del negocio | 2 | apps/api/marketplace | pendiente | BE-T016, ING-T007 | p95 por debajo de **500 ms** sobre el seed a escala; el filtro «disponibilidad real» usa **el mismo motor** que la reserva, no una copia |
| BE-T018 | **Ranking y patrocinados** (MKT-3, MKT-4, ADR-0009): puntuación única con las señales normalizadas, **pesos, radio, ventanas y duración del boost en `ranking_weights`** con fecha de vigencia; señales caras **precalculadas** en un trabajo periódico; **desglose guardado** para poder explicar por qué salió cada resultado; y los patrocinados resueltos **aparte e intercalados**, máximo 2 de cada 10 | 2 | apps/api/marketplace | pendiente | BE-T017 | **No hay ni un número de ranking en el código**; cambiar un peso desde el back-office cambia el orden **sin desplegar**; un negocio recién registrado **aparece**, porque el boost existe; los patrocinados **se insertan, no sustituyen** |
| BE-T019 | **Reviews y favoritos** (REV-1 a REV-6, MKT-5): review **solo con reserva completada**, una por reserva y con ventana de 14 días por defecto; rating 1–5 con texto y fotos; respuesta del negocio; reporte y cola de moderación; **rating agregado bayesiano** con `m` y `C` configurables y **`m` sembrada**; favoritos y compartir perfil | 2 | apps/api/marketplace | pendiente | BE-T010, ING-T007 | Una review sin reserva completada detrás **no existe por ninguna vía**; una review de 5 estrellas **no adelanta** a un negocio con ochenta de 4,7; **el patrocinio no toca el rating ni las reviews** |
| BE-T020 | **Protección del teléfono y del catálogo** (NEG-1, §12 del brief): click-to-chat **resuelto en servidor** con registro del clic y redirección, serializadores explícitos en toda respuesta pública, y **límites de ritmo** en OTP, búsqueda y escritura, por usuario y por IP | 2 | apps/api/marketplace, apps/api/contrato | pendiente | BE-T017 | **Ningún número de teléfono viaja** en un listado ni en un perfil público; recorrer el catálogo a ritmo de scraping **se corta**; ninguna respuesta devuelve el modelo entero |
| BE-T021 | **Impresiones y clics** (MKT-8) **agregados por día y negocio**, no fila por evento | 2 | apps/api/marketplace | pendiente | BE-T018 | Se puede pintar la serie de un negocio sin recorrer millones de filas de evento |
