# Tareas: Testing — **Estado: sin iniciar**

> Derivadas de la guía de lanzamiento por fase (`../README.md` §3). **TST-T003 y TST-T004 se escriben ANTES de que exista el motor de disponibilidad**: es el orden que pide el encargo, y no es negociable.
> Estados de tarea: `pendiente` · `en curso` · `bloqueada` · `hecha` · `validada`.
> Todo corre contra un **PostgreSQL real**. **Nada de SQLite**: aquí se prueban exclusiones, RLS y PostGIS, y nada de eso existe ahí.

| ID | Descripción | Fase | Zona | Estado | Depende de | Criterio de aceptación |
|---|---|---|---|---|---|---|
| TST-T001 | **Andamiaje de pruebas** con pytest contra un PostgreSQL real: fixtures de base limpia por prueba, datos de partida derivados del seed, y **`TZ=UTC` en el proceso** para que un despiste de huso local no pase desapercibido (ADR-0003, ADR-0014) | 1.a | apps/api/pruebas | pendiente | DEV-T006 | **Apagando la base de datos, el corredor falla en rojo.** No se salta nada en silencio: ese es el fallo conocido que esto evita |
| TST-T002 | **[CRÍTICA] Aislamiento entre negocios** (garantía 1, ADR-0002): con el negocio A fijado, **ninguna consulta a ninguna tabla devuelve filas de B**, ejecutada **con el rol real de la aplicación** y **con el filtro del código desactivado a propósito**. Más el caso de ONB-3: un profesional con membresía en A y en B **no ve la agenda de B** desde la sesión de A. Se escribe **vacía en 1.a** y se llena en 1.b | 1.a → 1.b | apps/api/pruebas | pendiente | BE-T002 | La prueba **falla** si se le da al usuario de la aplicación un rol con `BYPASSRLS`. Comprobado provocándolo, no supuesto |
| TST-T003 | **[CRÍTICA] Las pruebas del motor de disponibilidad, escritas antes que el motor.** Los ocho casos del encargo: dos confirmaciones del mismo hueco, **buffer que cruza el final de la jornada**, profesional con **horario distinto del negocio**, **cambio del horario del negocio con reservas dentro**, **servicio más largo que el hueco** antes del cierre, **multi-servicio encadenado** que necesita bloque continuo, **bloqueos recurrentes frente a puntuales**, y la zona horaria con `America/Panama` sin horario de verano pero guardando en UTC | 1.d | apps/api/pruebas | pendiente | ING-T003 | Las ocho pruebas existen **antes que BE-T009**, con nombre legible y resultado esperado explícito; **la lista de casos cubiertos se lee sin abrir el código**, porque es lo que se le enseña a Luis en la puerta |
| TST-T004 | **[CRÍTICA] Concurrencia real** (garantía 2, ADR-0004): **dos transacciones simultáneas de verdad** contra un Postgres de verdad confirmando el mismo hueco. Una crea la reserva, la otra recibe **`SLOT_NO_DISPONIBLE` con 409**. Repetida **con la comprobación de código desactivada**, para demostrar que la garantía la da la base. Más los bordes: cita que acaba a las 10:00 y otra que empieza a las 10:00 **no chocan**; con buffer de 15 minutos **sí**; y un **bloqueo de almuerzo impide** reservar encima | 1.d | apps/api/pruebas | pendiente | BE-T008 | La prueba **falla** si se elimina la restricción de exclusión, **aunque el `if` del código siga en su sitio**. Ese es el punto |
| TST-T005 | **Prueba de catálogo del aislamiento**: recorrer el esquema y **fallar si aparece una tabla con `business_id` y sin política de RLS**. Es la red contra la migración nueva que abre un agujero silencioso | 1.b | apps/api/pruebas | pendiente | TST-T002 | **Caza** una tabla nueva sin política, comprobado añadiendo una a propósito. **No se desactiva nunca «temporalmente»** |
| TST-T006 | **Prueba del contrato** (ADR-0012): el OpenAPI generado se compara con el confirmado y **falla si cambia sin querer**; y una prueba que fija que **los enumerados viajan en minúsculas con guion bajo** — en esta casa ya se rompió un front comparando en minúsculas lo que llegaba en mayúsculas | 1.a | apps/api/pruebas | pendiente | BE-T001 | Renombrar un campo **rompe la prueba**, no la producción; ningún enumerado sale en mayúsculas |
| TST-T007 | **Idempotencia** (garantía 6, ADR-0007, ADR-0008): ejecutar **dos veces seguidas** cada trabajo programado deja el mismo estado; el planificador ejecutado dos veces **no encola dos filas**; y repetir un `POST` de reserva con la misma `Idempotency-Key` **no crea una segunda cita**. Los trabajos se prueban **llamando a la función directamente**, sin Redis: se prueba el efecto, no el transporte | 1.f | apps/api/pruebas | pendiente | BE-T013 | **Un recordatorio duplicado a las siete de la mañana es una queja**, y esta prueba es lo que lo impide |
| TST-T008 | **El ciclo de la reserva** (RSV-1 a RSV-7): las transiciones válidas y **las que no lo son**, cancelación que libera el hueco **sin borrar la fila**, reprogramación que deja rastro, ventana de cancelación del cliente, y auto-confirmación **y camino manual**, los dos | 1.e | apps/api/pruebas | pendiente | BE-T010 | Ninguna transición inválida es posible por ninguna vía; cambiar el buffer de un servicio **no reescribe** las reservas ya creadas |
| TST-T009 | **Identidad** (ADR-0006): OTP caducado, intentos agotados, código anterior invalidado al emitir uno nuevo, **reutilización de un refresco ya rotado que invalida toda la familia**, y enlace social **rechazado si el correo no viene verificado** | 1.b | apps/api/pruebas | pendiente | BE-T003 | Cada uno de esos cinco casos tiene su prueba, y todas se han visto **fallar** rompiendo el código a propósito |
| TST-T010 | **Recorrido completo de punta a punta** (§6 del brief): registrar un negocio, publicarlo, reservar, cancelar y dejar una review. Es el guion de las capturas que se le entregan a Luis al cerrar cada fase | 2 | apps/api/pruebas | pendiente | BE-T019 | El recorrido pasa entero **con datos del seed**, y de él salen las capturas a 390 px y en escritorio |
| TST-T011 | **Rendimiento** (§6 del brief): disponibilidad p95 por debajo de **300 ms** y búsqueda del marketplace p95 por debajo de **500 ms**, medidos sobre un volumen que se parezca a la escala v1, no sobre tres filas | 2 | apps/api/pruebas | pendiente | BE-T017 | Los dos números están **medidos**, no estimados, y se dice con qué volumen |
