# Tareas: Mockuper — **Estado: sin iniciar**

> Derivadas de la guía de lanzamiento por fase (`../README.md` §3). **Todo lo de este backlog va por delante del Frontend**: una pantalla sin prototipo no se construye.
> Estados de tarea: `pendiente` · `en curso` · `bloqueada` · `hecha` · `validada`.
> **`packages/tokens` es zona serializada.** Nunca dos agentes a la vez.

| ID | Descripción | Fase | Zona | Estado | Depende de | Criterio de aceptación |
|---|---|---|---|---|---|---|
| MCK-T001 | **`packages/tokens`, la fuente única** (ADR-0013): JSON del que se generan variables CSS **y un módulo TypeScript para React Native**. Color **por función** —`superficie`, `superficie-elevada`, `borde`, `texto`, `texto-suave`, `acento`, `peligro`, `aviso`, `exito`—, los **cinco estados de la reserva** con color propio y estable, escala tipográfica de un eje con **mínimo 16 px** en cuerpo, **IBM Plex Sans con cifras tabulares**, radios contenidos, sombras solo de elevación real, y objetivo táctil de **44 px**. **Modo claro por defecto y modo oscuro preparado en tokens, no construido** | 0 | packages/tokens | pendiente | — | Contraste **AA verificado, estados incluidos**; ninguna fuente vetada; **los tokens se pueden consumir desde React Native sin depender de CSS**; añadir la paleta oscura después **no obliga a tocar un componente** |
| MCK-T002 | Prototipo del **alta de negocio a 390 px** (ONB-2, ONB-6, D11): datos, categorías, ubicación con pin, horario, primer servicio y foto, con **el checklist de lo que falta para publicar visible en todo momento**. Objetivo declarado: **menos de diez minutos desde el móvil** | 0 | docs/diseno, mockups | pendiente | MCK-T001 | Se navega entero a 390 px; en cada paso se ve qué falta, **no un error genérico al final**; el recorrido se cronometra y baja de diez minutos |
| MCK-T003 | Prototipo de **la agenda: día y semana** (AGD-2) — **la pantalla que más se usa del producto**, y la que decide si un dueño de salón se queda. Con la **agenda medio llena** y datos reales, la fila de cita como objetivo táctil entero, los cinco estados con su color, y **cancelar separado de mover** | 0 | docs/diseno, mockups | pendiente | MCK-T001 | Se usa con **una sola mano a 390 px**; las columnas de horas se alinean; con la agenda llena **se sigue leyendo de un vistazo** |
| MCK-T004 | Prototipo de **reserva manual, reprogramación y ficha de cliente** (AGD-2, RSV-5, RSV-6): apuntar a alguien que acaba de entrar por la puerta, moverlo de hora, marcar no-show, y ver su historial y sus notas | 0 | docs/diseno, mockups | pendiente | MCK-T003 | Apuntar un walk-in se hace **en segundos y en el móvil**; marcar no-show está donde tiene que estar y **no al lado de cancelar** |
| MCK-T005 | Prototipo del **marketplace**: búsqueda con filtros, ficha pública del negocio y **flujo de reserva del cliente en tres pantallas** tras elegir servicio (RSV-1, NEG-3), con la **política de cancelación visible antes de reservar** y los patrocinados **etiquetados e intercalados** | 2 | docs/diseno, mockups | pendiente | MCK-T001 | Se cuentan **tres pantallas**, no cinco; el botón de reservar está **siempre visible**; los patrocinados se distinguen **sin leer la letra pequeña** |
| MCK-T006 | **Los estados que nadie prototipa**: vacío, cargando, error, sin conexión y **3G lento**. Incluye qué se ve cuando el hueco se acaba de ocupar mientras el cliente confirmaba | 0 | docs/diseno, mockups | pendiente | MCK-T003 | Cada pantalla del backlog tiene sus cuatro estados dibujados; el mensaje de hueco ocupado **se entiende sin saber lo que es un 409** |
| MCK-T007 | **`packages/ui`**: los componentes compartidos entre web y back-office que salen de los prototipos, **sin lógica de negocio dentro** | 1.e | packages/ui | pendiente | MCK-T003 | Un componente de `packages/ui` se puede usar en las dos aplicaciones sin arrastrar reglas de negocio; **móvil no consume este paquete**, solo los tokens |
